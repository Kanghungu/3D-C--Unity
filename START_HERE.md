# START_HERE.md

## 이 문서의 역할
- 집에서 AI에게 작업을 맡길 때 가장 먼저 보여주는 실행용 문서다.
- 코드를 보기 전에 먼저 읽고, 현재 프로젝트의 방향과 작업 태도를 맞추는 용도다.
- 로컬에 미커밋 작업이 있을 수 있으므로, AI는 기존 변경을 함부로 덮어쓰면 안 된다.

## 먼저 읽을 문서
1. `AGENTS.md`
2. `START_HERE.md`
3. `SESSION_CONTEXT.md`
4. `TODO.md`
5. `DEVLOG.md`
6. `HOME_AI_PROMPTS.md`
7. `HOME_AI_HARNESS.md`
8. 필요할 때 `Docs/Reference/BATTLEFIELD_DIAGRAM.md`, `Docs/Reference/SYSTEM_FLOW_DIAGRAM.md`, `Docs/Reference/ANIMATION_STATE_DIAGRAM.md`, `Docs/Reference/ART_DIRECTION.md`, `Docs/Reference/VISUAL_REFERENCE_LINKS.md`

## 집에서 작업 시작 전에
- 가능하면 PowerShell에서 `./scripts/Start-HomeWork.ps1`를 먼저 실행한다.
- 이어서 `./scripts/Show-CurrentContext.ps1`를 실행해 현재 방향과 `task_brief.json`을 다시 확인한다.
- 그 다음 `HOME_AI_PROMPTS.md`의 프롬프트를 복붙해서 AI 작업을 시작한다.

## 지금 상태
- Unity 기반 3D SF RTS 전투 프로토타입이 이미 플레이 가능한 상태다.
- 카메라, 선택, 이동, 공격, 생산, 증원, HUD, 승패 루프가 연결되어 있다.
- 지상전 메인 프로토타입이 우선이며, 함대전은 별도 실험 씬으로 분리해 두었다.

## 프로젝트 한 줄 요약
- Unity 기반 3D SF 싱글플레이 RTS 전투 프로토타입
- 메인 목표는 `지상전 RTS 전투 루프 완성`
- 함대전은 `별도 실험`

## 지금 꼭 기억할 방향
- 완성작보다 `직접 조작해서 끝까지 진행되는 전투 프로토타입`이 우선이다.
- 메인 라인은 지상전이다.
- 없는 시스템을 상상해서 과설계하지 않는다.
- 문명 차이, 전장 가독성, 생산 위치, 카메라 감각, 미니맵 감각, 비주얼, 애니메이션 체감이 중요하다.
- 지금 단계에서는 큰 리팩터링보다 Unity에서 바로 확인 가능한 작은 수정이 우선이다.

## 현재 구현 상태 요약
- `SampleScene` 기준으로 메인 지상전 프로토타입이 돌아간다.
- 카메라 이동, 회전, 줌이 있다.
- 단일 선택, 드래그 선택, 더블클릭 선택이 있다.
- 우클릭 이동, 적 우클릭 공격, `A + 우클릭` 공격 이동이 있다.
- 핵심 보병 삼각은 `창병`, `방패 보병`, `총병` 기준으로 관리한다.
- 지원 병종으로 `포병`, `특수전사`, `친위대`, `전투기`, `이동 거점`, `비행 거점`이 있다.
- 플레이어 생산 건물과 랠리 흐름이 있다.
- 적은 자동 증원 흐름이 있다.
- 중앙 목표물 `Control Node`가 있다.
- HUD, 승패, 재시작 흐름이 있다.
- `FleetExperimentScene`은 별도 실험 씬이다.

## 지금 부족한 핵심 포인트
- 전장이 아직 테스트맵 느낌을 완전히 벗어나지 못했다.
- 플레이어/적 진영 차이가 더 선명해야 한다.
- 유닛 비주얼과 애니메이션이 더 살아 있어야 한다.
- 미니맵은 스타2처럼 "현재 보는 화면 범위"가 보이는 느낌이 아직 아니다.
- 맵 구조는 아직 단순하다.
- 메인 씬과 빌드 기준이 더 명확해질 필요가 있다.
- 승패 규칙과 HUD 문구도 경계 상황까지 더 직관적으로 다듬을 여지가 있다.

## 현재 핵심 방향
- 첫 맵은 `사막 성지` 테마다.
- 플레이어는 `중장갑 성전 제국` 방향이다.
- 적은 `정면 화력형 문명` 방향이다.
- 보병 전투 핵심은 `창병 / 방패 보병 / 총병` 삼각 구조다.
- 확장 병종은 핵심 삼각 위에 얹는 형태로 유지한다.

## 최근 비주얼 목표
- 전장은 단순히 넓기만 한 맵이 아니라, 전쟁 중인 장소처럼 밀도 있고 화려해야 한다.
- 참호, 방벽, 크레이터, 파괴된 길, 화염, 연기, 잔해, 큰 랜드마크가 있어야 한다.
- 유닛은 단순 프리미티브가 아니라, 역할별로 실루엣과 디테일이 살아 있는 멋있는 병기처럼 보여야 한다.
- 보병, 포병, 차량, 전투기, 함선이 서로 다른 전쟁 병기처럼 읽혀야 한다.
- 가능하면 정상 상태와 파괴 상태, 잔해 상태 차이도 고려하면 좋다.

## 레퍼런스 이미지 사용 기준
- 사용자가 올린 이미지뿐 아니라, 필요하면 AI가 직접 레퍼런스 이미지를 찾아도 된다.
- 다만 무작정 많이 찾지 말고, 현재 작업에 가장 도움이 되는 것만 골라야 한다.
- 맵, 보병, 차량, 함선, 전체 분위기처럼 카테고리별로 정리하면 좋다.
- 왜 참고하는지 짧게 설명해야 하고, 그대로 복제하지 말고 방향을 선명하게 만드는 용도로 써야 한다.
- 시작점으로는 `Docs/Reference/VISUAL_REFERENCE_LINKS.md`를 먼저 참고한다.

## 맵/전장 구조를 해석하는 법
- 현재는 `맵 > 필드 > 전장` 같은 큰 계층 구조가 없다.
- 메인 지상전 씬 전체를 현재 전장으로 보면 된다.
- 내부 구분은 대체로 `Ground`, `Terrain Features`, `Structures`, `Units` 정도다.
- 함대전은 이 구조와 별개인 별도 실험이다.
- 없는 월드맵 구조를 상상해서 새 시스템을 크게 만들지 말아야 한다.

## 바로 이어갈 때 체크할 것
- 승패 조건이 문서 기준과 일치하는지
- 플레이어/적 문명 차이가 전투에서 읽히는지
- 생산과 랠리, 거점 흐름이 처음 보는 사람에게도 이해되는지
- 지상전 메인 흐름을 망치지 않고 함대전 실험이 분리되어 있는지
- 전장이 여전히 비어 보이지 않는지
- 유닛 하나만 봐도 역할과 멋이 읽히는지

## 지금 AI에게 맡기기 좋은 작업 우선순위
- 가장 먼저 볼 작업은 `미니맵 화면 범위 표시`, `승패 경계 상황 정리`, `총공격 해금 규칙 단순화`다.
- 그 다음은 `전장 구조 재배치`, `생산/랠리 가독성`, `핵심 보병 삼각 가독성`, `HUD 문구 정리`다.
- 비주얼 쪽에서는 `전장 밀도 강화`, `기준 유닛 모델 디테일 강화`, `핵심 구역 랜드마크 강화`를 우선한다.
- `확장 병종 비중 재조정`, `환경 조형 강화`, `빌드 세팅 정리`는 그 다음 단계로 미룬다.
- 집에서 AI에게 맡길 때는 먼저 `TODO.md`의 `현재 추천 작업 10개`와 `지금 바로 할 것`을 기준으로 작업을 고르게 한다.

## 집에서 AI가 꼭 지켜야 할 작업 태도
- 문서부터 읽고 시작한다.
- 메인 목표가 지상전 전투 프로토타입이라는 점을 놓치지 않는다.
- 함대전을 메인 개발 라인처럼 다루지 않는다.
- 로컬 미커밋 작업이 있을 수 있으므로 기존 변경을 되돌리거나 크게 덮어쓰지 않는다.
- 작은 작업 1개씩 끝내되, 시작한 작업은 가능한 범위에서 `구현 -> 간단 검증 -> 문서 반영`까지 이어서 처리한다.
- 문서 정리 턴이면 코드보다 문서를 먼저 맞춘다.

## 작업 원칙
- 프로토타입이므로 작동과 가독성을 먼저 본다.
- 다만 큰 그림을 벗어날 정도의 기능 확장은 피한다.
- 새 기능을 넣으면 `DEVLOG.md`에 현재 구현 상태를 남긴다.
- 아트 개선도 무작정 전체 교체보다 `기준 유닛`, `핵심 전장 구역`, `핵심 구조물`부터 잡는다.

## AI가 자꾸 중간에 멈출 때 꼭 넣어야 할 지시
- "분석만 하지 말고 실제로 끝까지 구현해라"
- "한 단계만 하고 멈추지 말고, 이번 턴 안에서 가능한 데까지 계속 진행해라"
- "질문이 꼭 필요하지 않으면 묻지 말고 합리적으로 가정하고 진행해라"
- "작업 후 멈추지 말고 검증, 문서 반영, 다음 연결 작업까지 이어서 진행해라"
- "브레인스토밍이 아니라 실제 수정과 검증이 목표다"
- "이번 턴에서는 중간 보고 후 멈추지 말고 완료 가능한 범위까지 밀어라"

## 집에서 AI에게 기본으로 붙여넣는 추천 프롬프트
```text
`AGENTS.md`, `START_HERE.md`, `SESSION_CONTEXT.md`, `TODO.md`, `DEVLOG.md`를 먼저 읽고 현재 방향을 5줄 이내로 요약해줘.
그 다음 로컬에 미커밋 작업이 있을 수 있다는 점을 고려해서 기존 변경을 덮어쓰지 않는 가장 안전한 작업부터 진행해줘.
중간에 한 단계만 하고 멈추지 말고, 이번 턴 안에서 가능한 범위의 구현, 간단 검증, 필요한 문서 반영까지 계속 이어서 진행해줘.
질문이 꼭 필요하지 않으면 멈추지 말고 합리적으로 가정해서 끝까지 작업해줘.
메인 목표는 지상전 RTS 전투 프로토타입이고, 함대전은 별도 실험이야.
없는 시스템을 상상해서 크게 설계하지 말고 지금 소스와 문서에 맞는 범위만 다뤄줘.
```

## 속도가 느릴 때 더 강하게 쓰는 프롬프트
```text
이번 작업은 브레인스토밍이 아니라 실제 개발이 목적이야.
작업을 5~10분짜리로 끊지 말고, 최소 30분에서 1시간 정도는 한 흐름으로 계속 진행해줘.
한 단계만 하고 "다음 단계로 넘어갈까요?"라고 멈추지 말고, 지금 턴 안에서 가능한 범위의 구현 -> 간단 검증 -> 필요한 문서 반영까지 계속 진행해줘.
내가 별도로 멈추라고 하기 전에는 다음 자연스러운 하위 단계까지 이어서 작업해줘.
질문이 꼭 필요하지 않으면 묻지 말고 합리적으로 가정해서 진행해줘.
속도보다 중요한 건 중간에 안 끊기고 실제 진도가 계속 나가는 거야.
```

## 문서 정리만 시킬 때 쓰는 프롬프트
```text
이번 턴은 코드보다 문서 정리가 우선이야.
`AGENTS.md`, `START_HERE.md`, `SESSION_CONTEXT.md`, `TODO.md`, `DEVLOG.md`를 읽고,
현재 방향성과 우선순위가 흔들리지 않게 문서를 더 명확하게 정리해줘.
문서 정리 후에는 무엇이 핵심 문서이고 어떤 순서로 읽어야 하는지까지 정리해줘.
중간에 한 문서만 손보고 멈추지 말고 관련 문서 묶음을 한 번에 정리해줘.
```

## 집에서 AI에게 먼저 시킬 만한 일
1. 문서를 읽고 현재 방향을 짧게 다시 요약하게 하기
2. `TODO.md`의 `지금 바로 할 것` 안에서 가장 효과 큰 작업 1개를 고르게 하기
3. 미커밋 변경을 덮어쓰지 않도록 기존 상태를 먼저 존중하게 하기
4. 가능하면 그 작업을 구현, 검증, 문서 반영까지 한 번에 끝내게 하기

## 집에서 플레이 테스트할 때 확인할 것
1. 카메라 이동, 회전, 줌
2. 유닛 선택, 이동, 공격
3. 생산과 랠리 흐름
4. 전투 발생, 피해, 사망, 승패
5. 전장 가독성
6. 진영 차이
7. 유닛 비주얼과 애니메이션 체감
8. 전장이 비어 있는 테스트맵처럼 보이지 않는지
9. 유닛과 함선이 역할별 실루엣과 디테일을 가지는지
10. 미니맵이 있다면 카메라 위치가 아니라 화면 범위를 이해하는 데 도움이 되는지

## ?? ?? ?? ??
- ?? `Docs/Reference/LOCAL_VISUAL_GALLERY.md`? ??? ??, ??, ??, ?? ??? ????? ???.
- ? ?? `Docs/Reference/ART_DIRECTION.md`? `Docs/Reference/VISUAL_REFERENCE_LINKS.md`? ?? ??.
- ???? ???? ?? ? ?? ?? ?? ?? ??? ?? ??? ??? ??? ????.
## Codex-Only ?? ??
- ?? ????? `Codex??? ??? ???`?? ?? ????.
- ?? ?? ??, ? ?? ?, ????? ?? ?? ?? ?? ??? ?? ???.
- ????? `?? ??`, `?? ??`, `???`, `?? ??`, `??/??`, `?? ???`, `HUD`, `???`, `?? ?? ?? ?? ??`??.
- ?????? ?? ????? ??? ??? ???? ?? ? ???? ????.
- ??, ? ??, ?? ?????? ?? ????? ??? ?? ?? ??? ???? ????.
- ? ??? `?? ??`?? `??? ? ? ???? ? ??? ??? ?`? ????.

## ??? ?? ????? ??
- ?? ?????? ?? ????? ?? ?? ??? ? ?? ? ? ?
- ?? ??? ???? ?? ?? ??? ? ? ??? ? ?
- ??? ????? ????? "??? ???? ??? ? ??"? ??? ?? ?
- ????? ??? ??? ????? ??? ??? ?? ??? ??? ?
- ? ??? ?? ???? Codex??? ?? ??? ??? ?? ??? ????.
