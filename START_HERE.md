# START_HERE.md

## 지금 상태
- Unity RTS 전투 프로토타입이 이미 플레이 가능한 상태다.
- 카메라, 선택, 이동, 공격, 생산, 증원, HUD, 승패까지 기본 루프는 구현되어 있다.
- 지금부터는 "우리가 원한 그림"에 더 맞게 다듬는 단계다.

## 가장 먼저 볼 문서 순서
1. `SESSION_CONTEXT.md`
2. `TODO.md`
3. `DEVLOG.md`
4. 필요할 때만 `GDD.md`
5. 긴 흐름이 필요할 때만 `HISTORY.md`

## 지금 당장 중요한 것
- 승리 조건이 문서와 정확히 일치하는지 다시 맞추기
- 맵을 사막 성지 테마로 더 강하게 보이게 만들기
- 플레이어와 적 문명의 차이를 더 분명하게 만들기
- 함대전은 메인 프로토타입을 깨지 않는 별도 실험 씬으로 시작하기

## 오늘 작업 추천 순서
1. `SESSION_CONTEXT.md` 읽기
2. `TODO.md`에서 바로 할 작업 1개 고르기
3. AI에게 수정 요청
4. Play 모드 테스트
5. `DEVLOG.md`와 `HISTORY.md` 짧게 갱신
6. 커밋/푸시

## 지금 AI에게 먼저 시킬 만한 작업
1. `PrototypeMatchController.cs`에서 승리 조건을 `적 전멸 또는 적 기지 파괴`, 패배 조건을 `아군 전멸 또는 아군 기지 파괴`로 수정
2. `SampleScene`과 `PrototypeBootstrapper` 기준 전장을 사막 성지 테마로 정리
3. 플레이어는 `중장갑 성전 제국`, 적은 `정면 화력형`이 느껴지도록 유닛 차이 추가

## 작업할 때 주의
- 지상전 프로토타입이 우선이다.
- 함대전은 중요하지만 메인 프로토타입보다 먼저 완성하려고 하지 않는다.
- 문서보다 구현이 앞서면 문서를 바로 맞춰준다.
- 한 번에 너무 크게 바꾸기보다, Play 모드에서 바로 확인 가능한 단위로 나눈다.

