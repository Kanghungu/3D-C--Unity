# Docs

이 폴더는 **필수 문서가 아니다.** 루트의 `AGENTS.md`·`README.md`·`DEVLOG.md`가 우선이다.

## VisualBoards

`VisualBoards/` — 참고용 **SVG 보드**(유닛·전장 컨셉 스케치). 게임 런타임에서 자동 로드하지 않는다.

과거 `Docs/Reference/*.md` 기획 노트는 정리 과정에서 삭제했다. 방향·우선순위는 `AGENTS.md`와 `DEVLOG.md` 상단을 따른다.
