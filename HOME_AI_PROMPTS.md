# HOME_AI_PROMPTS.md

## 이 문서의 역할
- 집에서 AI에게 바로 복붙해서 쓰는 전용 프롬프트 모음이다.
- 기본 원칙은 `문서 먼저`, `중간에 멈추지 말기`, `미커밋 변경 덮어쓰지 말기`다.

## 사용 전 공통 규칙
- 먼저 `AGENTS.md`, `START_HERE.md`, `SESSION_CONTEXT.md`, `TODO.md`, `DEVLOG.md`를 읽게 한다.
- 메인 목표는 항상 `지상전 RTS 전투 프로토타입`이다.
- 함대전은 별도 실험이다.
- 없는 시스템을 상상해 과설계하지 않게 한다.
- 질문이 꼭 필요하지 않으면 멈추지 말고 진행하게 한다.

## 1. 기본 시작 프롬프트
```text
`AGENTS.md`, `START_HERE.md`, `SESSION_CONTEXT.md`, `TODO.md`, `DEVLOG.md`를 먼저 읽고 현재 방향을 5줄 이내로 요약해줘.
그 다음 로컬에 미커밋 작업이 있을 수 있다는 점을 고려해서 기존 변경을 덮어쓰지 않는 가장 안전한 작업부터 진행해줘.
중간에 한 단계만 하고 멈추지 말고, 이번 턴 안에서 가능한 범위의 구현, 간단 검증, 필요한 문서 반영까지 계속 이어서 진행해줘.
질문이 꼭 필요하지 않으면 멈추지 말고 합리적으로 가정해서 끝까지 작업해줘.
메인 목표는 지상전 RTS 전투 프로토타입이고, 함대전은 별도 실험이야.
없는 시스템을 상상해서 크게 설계하지 말고 지금 소스와 문서에 맞는 범위만 다뤄줘.
```

## 2. 끊기지 않고 30~60분 단위로 작업시키는 프롬프트
```text
이번 작업은 브레인스토밍이 아니라 실제 개발이 목적이야.
작업을 5~10분짜리로 끊지 말고, 최소 30분에서 1시간 정도는 한 흐름으로 계속 진행해줘.
한 단계만 하고 "다음 단계로 넘어갈까요?"라고 멈추지 말고, 지금 턴 안에서 가능한 범위의 구현 -> 간단 검증 -> 필요한 문서 반영까지 계속 진행해줘.
내가 별도로 멈추라고 하기 전에는 다음 자연스러운 하위 단계까지 이어서 작업해줘.
질문이 꼭 필요하지 않으면 묻지 말고 합리적으로 가정해서 진행해줘.
속도보다 중요한 건 중간에 안 끊기고 실제 진도가 계속 나가는 거야.
```

## 3. 코드 작업보다 문서 정리가 우선일 때
```text
이번 턴은 코드보다 문서 정리가 우선이야.
`AGENTS.md`, `START_HERE.md`, `SESSION_CONTEXT.md`, `TODO.md`, `DEVLOG.md`를 읽고,
현재 방향성과 우선순위가 흔들리지 않게 문서를 더 명확하게 정리해줘.
문서 정리 후에는 무엇이 핵심 문서이고 어떤 순서로 읽어야 하는지까지 정리해줘.
중간에 한 문서만 손보고 멈추지 말고 관련 문서 묶음을 한 번에 정리해줘.
```

## 4. 작은 작업 1개만 안전하게 시킬 때
```text
문서를 먼저 읽고, 현재 프로젝트에서 가장 안전하고 작은 작업 1개만 골라서 진행해줘.
로컬 미커밋 작업이 있을 수 있으니 기존 변경과 충돌하지 않는 범위를 우선해줘.
작업은 구현 -> 간단 검증 -> 필요한 문서 반영까지 한 번에 처리해줘.
중간에 멈추지 말고 그 작은 작업 묶음을 끝까지 마무리해줘.
```

## 5. 전장/진영/가독성 개선 쪽으로 시킬 때
```text
현재 메인 지상전 프로토타입 기준으로 전장 가독성과 진영 차이를 더 분명하게 만들어줘.
없는 시스템을 새로 크게 설계하지 말고, 지금 씬 구조와 문서 기준에 맞춰서 수정해줘.
플레이어와 적이 한눈에 구분되고, 전선 방향과 전장 목적이 읽히는 쪽을 우선해줘.
한 단계만 하고 멈추지 말고 구현, 간단 검증, 관련 문서 반영까지 이어서 진행해줘.
```

## 6. 비주얼/애니메이션 감각 개선 쪽으로 시킬 때
```text
현재 프로젝트의 큰 문제는 유닛과 전장이 아직 테스트맵 느낌이 강하다는 점이야.
스타크래프트 2, 헤일로 같은 밀리터리 SF 장갑 보병 느낌을 기준으로
플레이어 기본 보병 1종과 적 기본 보병 1종부터 비주얼과 애니메이션 감각을 개선해줘.
`Idle`, `Move`, `Attack`, `Hit 또는 Block`, `Death` 감각이 살아나는 방향을 우선해줘.
중간에 멈추지 말고 이번 턴 안에서 가능한 범위까지 계속 진행해줘.
```

## 7. 미니맵/카메라 감각 쪽으로 시킬 때
```text
현재 프로젝트에서 미니맵이 있다면 카메라 위치만 찍히는 느낌이 아니라,
스타크래프트 2처럼 메인 카메라가 현재 보고 있는 화면 범위가 이해되는 방향으로 정리해줘.
현재 코드에 미니맵 전용 시스템이 없다면 그 사실부터 먼저 짚고,
메인 전투 프로토타입에 바로 붙일 수 있는 최소 구현 기준으로 방향을 잡아줘.
중간에 설계만 하고 멈추지 말고 가능한 범위까지 계속 진행해줘.
```

## 8. AI가 자꾸 멈출 때 추가로 붙이는 한 줄
```text
중간 요약만 하고 멈추지 말고, 내가 멈추라고 할 때까지 다음 자연스러운 하위 단계로 계속 진행해줘.
```

## 9. 피해야 할 표현
- `속도 좀 내자`
- `빨리 해줘`
- `일단 이것만`

위 문장들은 추상적이라 AI가 다시 짧게 끊을 가능성이 높다.

## 10. 더 잘 먹히는 표현
- `이번 턴 안에서 가능한 범위를 끝까지 진행해줘`
- `한 단계만 하고 멈추지 말아줘`
- `구현 -> 검증 -> 문서 반영까지 한 번에 해줘`
- `질문이 꼭 필요하지 않으면 진행을 우선해줘`
- `30~60분 단위로 끊기지 않게 작업해줘`
