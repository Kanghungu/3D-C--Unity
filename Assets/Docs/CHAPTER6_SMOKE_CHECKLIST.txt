챕터 6 — 맵·미션 데이터 한 덩어리 스모크
========================================
**목표**: 미션 ID `mission_06_fortress` 가 **`DemoChapter6SingleMatchBundle`** 한 파일에서 표시명·목표 문구·덱·적 패턴을 읽고, 런타임에 **필드 스토리 트리거 박스**가 생기지 않는다(코어·지면·미션 루프만).

단일 소스
---------
- 코드: `Assets/Scripts/Campaign/Data/DemoChapter6SingleMatchBundle.cs`
- 여기만 수정하면: 표시 이름, 목표 한 줄/힌트, 8슬롯 덱, `EnemyPatternId`, 생산 간격 배율, 미러 여부, 공중 요새 덱 변주 여부, 필드 트리거 생성 여부

에셋 `Mission_06_Fortress`
--------------------------
- `missionId`·대사 ID·씬 이름·정렬 등 **참조** 유지
- `displayName` 은 번들과 맞춰 두었으나, **UI 표시는 `ResolveMissionDisplayName`** 이 번들 우선

플레이 체크
-----------
[ ] 캠페인 목록: **6. 요새 돌파 — 단일 데모 전장** | 요새 돌파
[ ] 상단 목표 바·F1·브리핑 폴백 문구가 번들 **목표/힌트**와 일치
[ ] 덱 4번 슬롯이 **공중 요새** 계열(번들 덱 + 공중 변주 규칙)
[ ] 적 패턴이 **`fortress_break`** 체감(이전에는 공중 변주만 켜면 `airborne_siege` 로 덮였음)
[ ] 씬에 **MissionStoryZone_** 트리거 오브젝트가 생기지 않음(계층 확인)

콘솔: 빨간 Error 없음.

이슈 메모: 씬명, 한 줄.
