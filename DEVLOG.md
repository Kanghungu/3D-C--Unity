# DEVLOG.md

## 1. 비전 초안 (플레이어 답변)

1. **한 줄**: 문명을 지키는 지휘관이 거점을 지키거나 빼앗는 SF 싱글 RTS.  
2. **기둥**: 미션 목표 종류.  
3. **기억에 남는 판**: 이단 거점만 부수면 이기는 판.  
4. **최소 품질**: 브리핑 없이도 목표가 보인다.  
5. **지금**: 짧은 캠페인으로 한 번 완주해 보기.

## 2. 집에서 확인할 일

아래는 **에디터 Play** 기준이다. 빌드 실행 파일로 확인할 때도 같은 항목을 보면 된다. 검증 끝난 줄은 지우거나 줄여도 된다.

### 준비

1. Unity로 프로젝트를 연다.  
2. `File → Build Settings` 에 **`CampaignMenu`**, **`NewSampleScene`** 이 포함되어 있는지 본다(없으면 `Add Open Scenes` 등으로 넣는다).  
3. **Console** 을 연다(`Window → General → Console`). **Clear** 해 두고 시작하면 로그 추적이 쉽다.

### A. 캠페인 메뉴 씬

1. `Assets/Scenes/CampaignMenu.unity` 를 연 뒤 **Play** 를 누른다.  
2. **콘솔에 빨간 에러가 없는지** 본다(노란 경고는 일단 메모만).  
3. 화면 상단 근처에 **「Progress [●] [○] …」** 형태로 **카탈로그에 등록된 미션 수만큼** 클리어 표시가 보이는지 확인한다.  
4. 한두 미션을 깬 상태라면 **●** 와 **○** 가 섞여 보이는지 확인한다.  
5. 화면 하단 **Reset Campaign Progress** 버튼을 눌렀을 때 진행·완료 표시가 초기 상태로 돌아가는지 확인한다.

### B. 캠페인 → 전투 진입 (공통)

1. 메뉴에서 **미션 1** 을 선택해 `NewSampleScene` 으로 들어간다.  
2. **브리핑 카드**가 뜨는지, 텍스트가 잘리지 않는지 본다.  
3. **Space / Enter / 좌클릭** 중 하나로 브리핑을 닫고 전투가 시작되는지 확인한다.  
4. 전투 시작 후 **상단 UGUI 목표 바**에 미션 이름·목표 한 줄·보조 설명이 보이는지 확인한다.  
5. **좌측 상단 IMGUI**에 **자원·덱·생산 큐** 안내가 **한글**로 읽히는지 확인한다.  
6. **전투 중 콘솔에 빨간 에러가 새로 뜨지 않는지** 가끔 본다.

### C. 전투 중 추가로 볼 것

1. **미니맵**: 클릭·드래그 패닝. **Shift + 좌드래그** 박스로 **아군만** 선택되는지.  
2. 아군 **한 유닛만** 선택 시 **아군 코어와 청록 연결선**. 적 코어 저체력 시 점 색·크기 강조.  
3. **우클릭 불가** 지면: 짧은 거절음 + **하단 한 줄** 안내.  
4. **아군 피격**: 짧은 붉은 플래시, 병종별 피격 톤 차이 감으로.  
5. **O**: 설정(마스터·전투·화면), **UI 크기**, 저장 후 Play 재시작 유지, 짧은 창에서 **스크롤**.  
6. **V**: 표적 모드, 재시작 후 유지.  
7. **F1**: 한글 안내, **핵심 5줄** + **자세히 보기**.

### J. 안개 전쟁(FoW)·시야 *(짧게)*

1. 브리핑 종료 후 지형에 **미탐색 / 탐색(옅은 안개) / 시야 안(맑음)** 이 구분되는지.  
2. **미니맵**에도 안개, **유닛·코어 점은 위**에 그려지는지.  
3. **F10**: FoW 디버그 격자, **장애물LOS** 문구(`BattleAcesFogOfWarDebug` 에서 끄기 가능).  
4. **미션 5** 등: 지상 시야는 장애물 뒤 잘 안 열리는지, **공중/비행**은 넓고 넘어 보이는지.  
5. Console에 **`[FoW] Shader … 찾을 수 없습니다`** 없는지.

### D. 승리·패배·결과 화면

1. 승·패 카드, **「종료 사유: …」** 한 줄.  
2. 결과 시 **상단 목표 바 숨김**, **금색 R 안내**, **메뉴에서 다음 미션** 회색 줄.  
3. **패배** 시 **재시도 힌트** 한 줄. 본문·통계 줄 **겹침 없음**(저해상도).

### E. 미션별 (시간 있을 때)

| 미션 | 확인 포인트 |
|------|-------------|
| 1 스커미시 | 적 코어 격파 승리. **약 60초** 개장 자원 완화. 필드 대사 2구역. |
| 2 성역 방어 | **남은 시간 ~120초** 표시·카운트다운, 종료 시 승리. 필드 대사 2구역. |
| 3 성유물 호위 | 성유물 HP·실패 조건, 목표 구역 대사. |
| 4 이단 본거지 | 표식 구조물 파괴 승리, 체렐 감. |
| 5 거점 점령 | 톤·점령·장애물, 필드 대사 2번. FoW는 **J**. |
| 6 이동/공중 요새 | `airborneCitadelFocus` 시 `*_air` 대사·덱 공중 요새, 필드 2구역. |

### F. 밸런스·가독성 (감으로)

1. 초반 자원 속도. 2. 미션 2 방어 시간 체감. 3. HUD 글자 가독성.

### G. 문제 재현 시 적어 둘 것

씬(메뉴/전투), 재현 순서, 콘솔 복사·스크린샷, 전체 화면 1장 — 아래 **§4 버그 메모**에도 한 줄 남겨도 된다.

### H. 선택: PC 빌드

`Build Settings` → Windows **Build** → 실행 파일로 `CampaignMenu` 부터 동일 플로우. 에디터와 다르면 **빌드 전용** 이슈로 메모.

### I. 집 스모크 (짧게)

1. Console **Clear** → `CampaignMenu` Play → 노란 경고 과다 여부.  
2. 미션 1 → 2~3분 → 에러 없음. (여유 시 **J**.)  
3. 미션 2 → **약 120초** 방어.  
4. 승 또는 패까지 → 상단 바 숨김·결과 카드 레이아웃(**D**).  
5. **Tools → 프로젝트 → 빌드 씬 목록 검사** → **Build** → 미션 1만 실행해 화면·입력·소리.

---

## 3. 전체 진행 요약

| 파트 | 상태 | 메모 |
|------|------|------|
| 전투 코어 루프 (카메라·선택·이동·공격·체력·승패) | 완료 | `AGENTS.md` 3절 기준 충족 |
| 캠페인 메뉴·미션 카탈로그·진행 저장(●○·보너스·F1 1회 안내) | 완료 | `CampaignMenu` · `CampaignProgressStorage` |
| Battle Aces 전투 씬·미션별 목표·브리핑·승패 카드 | 완료 | 카탈로그에 등록된 미션 수는 에셋 기준(6+ 등) |
| HUD·설정(O)·F1·미니맵·피드백(우클릭 거절·피격·플래시) | 완료 | `ImGuiGameUi` · UGUI 목표 바 |
| 수입·적 AI 밸런스 | 진행 중 | `ApplyMissionIncomeTuning` · `EnemyBrain` — 플레이 감에 따라 숫자만 조정 |
| FoW 1차 (지형·미니맵·탐색 안개·장애물 LOS·공중 시야) | 완료 | 확인은 **§2 J** |
| FoW 2차 (유닛 메시 가림·정책) | 예정 | 필요 시 |
| 승패·목표 UX 타이밍·겹침 | 진행 중 | 대규모 UI 리팩터는 보류 |
| 에디터 빌드 검증·Windows 빌드 스모크 | 진행 중 | **§2 I** · `scripts/Build-Windows.ps1` 참고 |
| 구형 `SampleScene` + `PrototypeBootstrapper` | 참고용 | 새 기능은 Battle Aces·캠페인 우선 |

**미루는 것** (`AGENTS.md` 4절): 멀티, Steam, 서버, 오픈월드·대서사·대규모 경제/외교, 과최적화·셰이더 집착, 대량 에셋, 과한 추상화.

---

## 4. 버그·이슈 메모

*(발견 시 날짜·한 줄로 적고, 고친 뒤 지우거나 취소선.)*

- 

---

## 5. 최근 요약

- FoW: 지형·미니맵 마스크, 탐색 옅은 안개, `VisionObstacle` LOS, 공중 시야 확장·F10 디버그.  
- 캠페인·전투: 대사·승패·브리핑 UX, 수입/방어 시간·적 AI 소조정, 미션6 공중 변주 등.  
- 상세 커밋·장문 로그는 **Git 히스토리**가 원본.
