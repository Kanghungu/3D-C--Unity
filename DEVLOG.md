# DEVLOG.md

## 현재 진행 (요약)

1. **진행 축**: `CampaignMenu` → 미션 선택 → `NewSampleScene`(Battle Aces: 단일 코어×2, 덱·자원·목표별 승패).
2. **캠페인 데이터**: `Assets/Campaign/Content` — 대사표·**미션 5종**·카탈로그. 목표 문구는 `MissionObjectiveDisplayText`와 브리핑 톤 통일.
3. **UI**: IMGUI(`ImGuiGameUi`) + UGUI 상단 목표(`BattleAcesObjectiveUgui`). 씬 로드는 `CampaignSceneLoadUtility`로 빌드 검증.
4. **구형 프로토타입**: `SampleScene` + `PrototypeBootstrapper` — 참고·실험용.
5. **제거됨**: 함대 실험(`Fleet`·실험 씬) — MVP 축소.
6. **설정/오디오**: 마스터 볼륨·카메라 감도·**V 표적 모드(PlayerPrefs)**·프로시저럴 효과음(외부 WAV 없음).
7. **문서**: 루트는 `AGENTS.md`·`README.md`·본 파일만 유지. 과거 장문 로그·`Docs/Reference` MD는 정리함(SVG는 `Docs/VisualBoards`).

## 비전 초안 (플레이어 답변)

1. **한 줄**: 문명을 지키는 지휘관이 거점을 지키거나 빼앗는 SF 싱글 RTS.  
2. **기둥**: 미션 목표 종류.  
3. **기억에 남는 판**: 이단 거점만 부수면 이기는 판.  
4. **최소 품질**: 브리핑 없이도 목표가 보인다.  
5. **지금**: 짧은 캠페인으로 한 번 완주해 보기.

## 최근 기록 (요약)

- **2026-04-06 (후반)**: Battle Aces UX·캠페인 보강 — 미니맵 **Shift+박스** 아군 선택, 코어 **생산 예고 한 줄**, 승패 **종료 사유 한 줄**, 캠페인 메뉴 **미션 1~5 클리어 ●/○**, 미션 5 거점 점령 **맵·적 패턴 변주**, `BattleAcesMatchController.TryGetInstance`·주석 정리, **V 표적 모드 저장**.
- **2026-04-06**: 함대 실험 제거, 지상전 MVP로 집중.  
- **2026-03 ~ 04**: RTS 기본 조작·생산·승패·유닛 정의·전장 확장·HUD 등 프로토타입 루프 구축(상세는 Git 히스토리 참고).  
- **문서 정리**: 루트·`Docs/Reference`의 중복·AI 보조용 MD 삭제, `DEVLOG` 본문 압축.

## 집에서 확인할 때 (체크리스트)

1. **실행**: `CampaignMenu` → **Play** → 콘솔에 **에러(빨강)** 없음.  
2. **캠페인 메뉴**: 상단 **「미션 클리어 ● ○」**(1~5) 표시·`진행 초기화` 동작.  
3. **전투 한 판**: 브리핑 → 전투 → 승 또는 패까지. 승패 카드에 **종료 사유** 한 줄.  
4. **미니맵**: **Shift + 좌드래그**로 박스 → 그 안 **아군만** 선택되는지.  
5. **하단 HUD**: **다음 유닛: … (n초)** (생산 중일 때).  
6. **V 키**: 표적 모드 전환 후 **에디터 재실행**해도 설정 유지되는지.  
7. **미션 5**: 거점 점령·레이아웃/구역이 이전 스텁과 다르게 느껴지는지(선택).

## 다음에 할 일 (여유 있을 때)

- 위 체크리스트 전 항목 통과·콘솔 경고 정리.  
- 필요 시 PC 빌드로 동일 플로우 재확인(선택).  

---

*이전에 본문에 쌓여 있던 일자별 장문 로그는 저장소 정리로 제거되었다. 필요 시 Git 히스토리에서 복원한다.*
