# DEVLOG.md

## 1. 비전 초안 (플레이어 답변)

1. **한 줄**: 문명을 지키는 지휘관이 거점을 지키거나 빼앗는 SF 싱글 RTS.  
2. **기둥**: 미션 목표 종류.  
3. **기억에 남는 판**: 이단 거점만 부수면 이기는 판.  
4. **최소 품질**: 브리핑 없이도 목표가 보인다.  
5. **지금**: **Battle Aces 한 판 데모**를 단단히 만든 뒤, 필요하면 캠페인 미션을 이어 붙인다.

## 2. 집에서 확인할 일

**에디터 Play** 기준. 빌드로도 같은 식으로 보면 된다.

**개발 초점은 데모**다. 회귀 순서는 **`CHAPTER1_SMOKE_CHECKLIST.txt`** 가 본전이며, RTS 코어·본진·덱·자원·생산·랠리는 **`CHAPTER2`·`CHAPTER3` 스모크**(같은 `Assets/Docs/`)로 덧댄다. 캠페인 미션 표(E절)는 **시간 날 때·캠페인 에셋 건드릴 때만**.

**절 순서**: 준비 → **챕터 1 스모크(데모)** → B 전투 진입(데모 우선) → C 입력·HUD → D 승패 → E 미션표(옵션) → F~I → J 안개.

### 준비

1. Unity로 프로젝트를 연다.  
2. `File → Build Settings`에 **`CampaignMenu`**, **`NewSampleScene`** 포함 여부(없으면 에디터 메뉴 **Game/Campaign/Register Campaign Scenes In Build** 또는 수동 추가).  
3. **Console**(`Window → General → Console`) 연 뒤 **Clear**.  
4. (권장) **Game/Campaign/Verify Chapter 1 Build Scenes** 또는 **Tools/프로젝트/챕터1 씬 (CampaignBuildMenu 와 동일)** 로 목록·파일 존재 확인(경로 단일 출처 `CampaignChapter1BuildPaths`).

### 챕터 1 — 진입·메뉴·난이도·스모크 (고정)

**항상 같은 순서**로 돌릴 것: 상세·콘솔 Error/Warning 구분·난이도 수치 표는 **`Assets/Docs/CHAPTER1_SMOKE_CHECKLIST.txt`**.

1. `CampaignMenu` Play → 메인 **캠페인/데모** → **← 메인** 복귀 → **데모** 한 판 끝까지 → 결과·재시작·메인 복귀.  
2. **`NewSampleScene`만** 연 채 Play → 폴백 데모 한 판 → 결과·메인.  
3. (가능 시) **빌드** 후 실행 파일로 데모 한 판만 재현.  
4. 위 전 구간에서 Console **Error(빨강) 0**. Warning 은 체크리스트의 「무시 가능 / 수정 권장」기준.

### B. 전투 진입 — 데모 우선, 캠페인은 옵션

**데모(본전)**  
1. `CampaignMenu` → **메인 → 데모** → 난이도 하나 → `NewSampleScene` · 브리핑 없음.  
2. **상단 UGUI 목표 바**·**좌상 IMGUI** 한글·전투 중 **빨간 에러** 없음.

**캠페인 미션(에셋·브리핑 회귀할 때만)**  
1. **미션 1**(또는 수정 중인 미션) 선택 → `NewSampleScene`.  
2. **브리핑 카드**·본문 잘림 없음.  
3. **Space / Enter / 좌클릭**으로 브리핑 종료·전투 시작.  
4. 목표 바·IMGUI 동일 기준으로 확인.

### C. 전투 중 입력·미니맵·기타

1. **일시정지·배속**: **P** 토글 일시정지. **`[` / `]`** 로 배속 한 단계씩(느림↔빠름). **숫자패드 - / +** 동일. *(카메라 Q/E 회전과 겹치지 않게 QWE 배속은 사용하지 않음.)*  
2. **생산 집결**: **Alt + 우클릭**(지면) — 아군 코어에서 나온 유닛 랠리. 청색 링 표시.  
3. **적 AI**: 생산 유닛은 먼저 **적 집결 지점**으로 모임 → 대개 **5기 전후**가 모이면 물결 공격(패턴·장기 대기 시 4기 완화).  
4. **미니맵**: **좌클릭** 시야 이동·**드래그** 패닝. **Shift + 좌 드래그** 아군만 박스 선택. **Ctrl + 좌클릭** 클릭 지점 **근처 아군** 선택(없으면 시야만 이동). 우하 **범례**에 Ctrl 안내 한 줄.  
5. 아군 **단일 선택** 시 코어까지 **청록 연결선**. 적 코어 저체력 시 점 강조.  
6. **우클릭 불가** 지형: 거절음 + 하단 한 줄.  
7. **아군 피격**: 붉은 플래시·병종별 톤(감으로). **지휘 코어** 피격은 별도 묶음(소리+플래시+상단 막대, unscaled 쿨다운).  
8. **생산·강화 피드백**: 덱 주문 **성공**(청록 막대+짧은 톤) vs **거절**(금색 막대+낮은 톤, 자원·큐·상한·브리핑 동일 패턴). T/Y/U 성공은 `PlayCoreUpgradeApplied`. 생산 완료 시 짧은 **링** 이펙트.  
9. **승패**: 스팅 직후 **unscaled 0.14s** 뒤 결과 카드·승리 카메라 당김 동기.  
10. **미니맵**: 클릭 시야는 매번, **링·톤**만 짧은 쿨다운.  
11. B축 상세 표: `Assets/Docs/BATTLE_ACES_IMMEDIATE_FEEDBACK.md`.  
12. **O**: 설정·UI 크기·저장 후 재시작 유지·좁은 창 **스크롤**.  
13. **V**: 표적 모드·재시작 후 유지.  
14. **F1**: **핵심 요약**(한·영) + **자세히**; 전투 중 **상단 목표 바가 켜지면** F1 에서는 주 목표 문구 **생략**·보조 힌트만(중복 방지).  
15. **개발 빌드·에디터**: **F10** — 우상단 진단(FPS·씬·timeScale·그래픽 프리셋·해상도). **F11** — FoW 디버그 격자 토글(`BattleAcesFogOfWarDebug`, F10 과 역할 분리). **일시정지 P**·**배속 [ ]·숫자패드 ±** 는 `RtsTimeControl` 과 일시정지 패널(`DemoPresentationCopy.PauseBattleControlsBody`) 문구와 동기화.  
16. **Player.log `[DemoBoot]`** — 첫 씬 로드 후 한 줄(버전·씬·해상도·`devBuild`·`editor`·`gfxPreset`·미션 ID). 빌드 스모크 비교용.  
17. **가독성(C)**: 자원 스트립·코어 HP 구간·미니맵 점·시간 한 줄 규칙은 `BattleAcesReadability`·`ImGuiGameUi`·`DemoPresentationCopy` — 표 `Assets/Docs/BATTLE_ACES_READABILITY.md`.

### D. 승리·패배·결과 화면

1. 승·패 카드, **종료 사유** 한 줄.  
2. 결과 중 **상단 목표 바 숨김**, **R** 안내, 다음 미션 회색 줄.  
3. **챕터 0(한 판 데모 루프)**: `NewSampleScene`만 단독 Play → `ActiveMission` 없을 때 **폴백 데모** 자동 설정 → 브리핑 없음·결과 카드·**같은 데모 재시작**·**메인 메뉴로**. 메뉴 **데모** 진입은 스커미시(쉬움·보통·어려움), 결과 버튼은 **메인 메뉴로**.  
4. 패배 시 **재시도 힌트**. 저해상도에서 본문·통계 **겹침 없음**.

### E. 캠페인 미션별 (캠페인 작업할 때만)

| 미션 | 확인 포인트 |
|------|-------------|
| 1 스커미시 | 적 코어 격파 승리. **약 60초** 개장 자원 완화. 필드 대사 2구역. |
| 2 성역 방어 | **남은 시간 ~120초**·카운트다운, 종료 시 승리. 필드 대사 2구역. |
| 3 성유물 호위 | 성유물 HP·실패 조건, 목표 구역 대사. |
| 4 이단 본거지 | 표식 구조물 파괴 승리, 체력 감. |
| 5 거점 점령 | 보라 구역 **아군 유지 시만** 타이머 진행·비우면 리셋. 구역 안 **소량 체력 회복**(기본값, `MissionCaptureZone.occupantHealPerSecond`로 조절·0이면 없음). 상단 목표 줄에 타이머·회복 설명. 장애물·톤·필드 대사. FoW는 **J**. |
| 6 이동/공중 요새 | `airborneCitadelFocus` 시 `*_air` 대사·덱 공성 등. **건물/거점 클릭 선택 없음** — **F1**에 공중 요새 안내 문단 표시. 생산 **1~8**, 명령 **우클릭** 동일. 필드 2구역. |

### F. 밸런스·가독성 (감으로)

초반 자원 속도, 미션 2 방어 체감, HUD 가독성.

### G. 문제 재현 시 적어 둘 것

씬, 재현 순서, 콘솔·스크린샷, 전체 화면 1장. **4절 버그 메모**에 한 줄 남겨도 됨.

### H. 선택: PC 빌드

`Build Settings` → Windows **Build** → **`CampaignMenu` → 데모** 플로우를 에디터와 동일하게. 캠페인 미션은 옵션.

### I. 집 스모크 (짧게)

1. Console **Clear** → `CampaignMenu` Play → **메인 → 데모** 한 판 → 결과·메인.  
2. (권장) `NewSampleScene` 단독 Play → 폴백 데모 한 판.  
3. 노란 경고 과다 여부 메모. 여유 시 **J**(FoW). 개발 빌드면 **F10/F11**·**O** 설정(Performance)에서 FoW 갱신 완화 확인.  
4. **Build** 후 실행 파일로 **데모 한 판**만(화면·입력·소리). Player.log **`[DemoBoot]`** 한 줄 확인.  
5. (캠페인 작업 시에만) 미션 1·2 등 **E절** 항목.

### J. 안개 전쟁(FoW)·시야 (짧게)

1. 지형: **미탐색 / 옅은 안개 / 시야 맑음** 구분.  
2. 미니맵에도 안개, 유닛·코어 점은 위에 표시.  
3. **F11**: FoW 디버그 격자·장애물 LOS 미리보기 토글(`BattleAcesFogOfWarDebug`). **F10**은 별도 — 전역 진단 HUD만(겹침 방지).  
4. **저사양 그래픽(Performance)**: 시야 그리드 갱신이 인스펙터 값과 무관하게 **최소 2프레임에 1회**로 완화(`GetEffectiveVisionUpdateInterval`). Balanced 는 인스펙터 `visionUpdateEveryNFrames` 그대로(기본 1).  
5. 미션 5 등: 지상 시야·장애물, 공중/비행 시야 넓음.  
6. Console에 **`[FoW] Shader … 찾을 수 없습니다`** 없음.

---

## 3. 전체 진행 요약

| 파트 | 상태 | 메모 |
|------|------|------|
| **한 판 데모 루프** (데모·폴백·결과·재시작·메인) | 진행/다듬기 | `CHAPTER1_SMOKE` · 챕터 0~1 |
| 전투 코어 루프 (카메라·선택·이동·공격·체력·승패) | 완료 | `AGENTS.md` 3절 기준 충족 |
| Battle Aces 전투·HUD·적 AI·난이도·수입 밸런스 | 진행 중 | 데모 체감 우선 · `EnemyBrain`·튜닝 |
| 메뉴(`CampaignMenu`)·**데모** 진입 | 완료 | 메인 → 데모 / 캠페인 |
| 캠페인 미션 카탈로그·진행 저장·브리핑 | 유지 | **옵션** — 미션 추가는 데모 이후 |
| HUD·설정(O)·F1·미니맵·피드백(우클릭 거절·피격·플래시) | 완료 | `ImGuiGameUi` · UGUI 목표 바 |
| FoW 1차 (지형·미니맵·탐색 안개·장애물 LOS·공중 시야) | 완료 | 확인은 **2절 J** |
| FoW 2차 (유닛 메시 가림·정책) | 예정 | 필요 시 |
| 승패·목표 UX 타이밍·겹침 | 진행 중 | 대규모 UI 리팩터는 보류 |
| 에디터 빌드 검증·Windows 빌드 스모크 | 진행 중 | **2절 I** · `scripts/Build-Windows.ps1` 참고 |
| 구형 `SampleScene` + `PrototypeBootstrapper` | 참고용 | 새 기능은 **Battle Aces** 우선 |

**미루는 것** (`AGENTS.md` 4절): 멀티, Steam, 서버, 오픈월드·대서사·대규모 경제/외교, 과최적화·셰이더 집착, 대량 에셋, 과한 추상화.

---

## 4. 버그·이슈 메모

*(발견 시 날짜·한 줄로 적고, 고친 뒤 지우거나 취소선.)*

- 

---

## 5. 최근 요약

- FoW: 지형·미니맵 마스크, 탐색 옅은 안개, `VisionObstacle` LOS, 공중 시야 확장·**F11** 디버그 격자·Performance 시 **2프레임마다** 시야 갱신 완화.  
- 캠페인·전투: 대사·승패·브리핑 UX, 수입/방어 시간·적 AI 소조정, 미션6 공중 변주 등.  
- 입력·안내: 배속 **`[`/`]`·숫자패드 ±**, 미니맵 **Ctrl+클릭**, **Alt+우클릭** 생산 랠리, 점령 **소량 회복**·공중 **F1** 안내. 적 **집결 후 물결 공격**(대개 **5기** 묶음). HUD·목표 바 **청록 스트라이프·카드형 레이아웃**.  
- 메뉴 **메인 → 데모** — **쉬움·보통·어려움**(스커미시 ID): 적 생산·개장 부스트만 다름. 브리핑 생략 · ●○ 미저장 · 결과 **메인 메뉴로**.  
- **한 판 데모 챕터 0**: `battle_aces_one_match_demo` 폴백, `FindOrCreateForBattleScene`, `NewSampleScene` 단독 Play 도 동일 루프.  
- **챕터 1(진입·메뉴)**: 메인에서 **데모 우선**·Esc 복귀·수치 **MissionDefinition** 단일 출처. 빌드 검증 `Verify Chapter 1 Build Scenes`, 스모크는 `CHAPTER1_SMOKE_CHECKLIST.txt` · 마무리 조건은 **DEVLOG 8절**.  
- 코어 기본 체력 **5200 / 4800** 상향.  
- 문서·우선순위: **데모 제작이 핵심** — README·AGENTS·CampaignManual·task_brief·빌드 스모크 정렬.  
- 상세 커밋·장문 로그는 **Git 히스토리**가 원본.

---

## 6. 집에서 빠른 확인 (한 패스)

에디터 없이 작업만 한 뒤 집에서 할 때 **순서만** 따라가면 된다.

1. **`CHAPTER1_SMOKE_CHECKLIST.txt`** 순서대로 — **데모 한 판** + (권장) `NewSampleScene` 단독 한 판.  
2. 결과 카드·**메인 메뉴로**·재시작·콘솔 **Error 0**.  
3. (옵션) 메뉴 **캠페인** ●○·미션 1 브리핑 — 캠페인 에셋 건드렸을 때만.  

자세한 항목: **2절 챕터 1·D항**, `Assets/Docs/CHAPTER1_SMOKE_CHECKLIST.txt`(본전). `CAMPAIGN_SMOKE_CHECKLIST.txt` 는 같은 폴더에서 **데모 요약 + 캠페인 6미션은 선택**만 적어 둠.

---

## 7. 한 판 데모 로드맵 — 챕터 0 마무리

- **완료로 보는 조건**: (1) 메뉴 **데모** 또는 **전투 씬 단독**으로 한 판이 끝까지 간다. (2) **R**·**같은 데모/스커미시 재시작**·**메인 메뉴로**가 동작한다. (3) 캠페인 ●○는 연습 경로에서 변하지 않는다.  
- **챕터 1 이후**는 별도 로드맵(입력·밸런스·UI·맵·연출 등)으로 쪼갠다 — Notion/이슈에 15~20챕터로 늘려도 된다.

---

## 8. 챕터 1 — 진입·메뉴 마무리

- **메인**: 데모 버튼을 위·먼저 배치, 캠페인은 옵션 문구.  
- **복귀**: 캠페인/데모 하위에서 **Esc** 또는 **← 메인 (Esc)**.  
- **수치 표기**: `MissionDefinition` 상수 + `BuildSkirmishDemoMenuStatsLine` / `FormatSkirmishThinkMultiplierForButton` 로 메뉴와 런타임 부스트·생산 배율 **단일 출처**.  
- **검증**: `CHAPTER1_SMOKE_CHECKLIST.txt` 2) 항목 전부 체크.
